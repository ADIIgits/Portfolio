Update the portfolio design to extend the cinematic background style to other sections.

IMPORTANT:
Do NOT modify the current hero section (the section containing "Aditya Sharma"). It is already correct and must remain exactly as it is.

Goal:
Apply a similar **cinematic smokey background + image blend effect** to the sections below the hero so the whole page feels visually cohesive.

Requirements:

1. Apply Background Image Sections

For major sections below the hero (Projects, Certificates, Education, Competitive Programming, etc.), add a **background image layer** that blends smoothly into the section using a soft smokey gradient.

The background image should appear subtly behind the section content.

2. Smokey Gradient Blend

Each section should use a gradient fade similar to the hero style:

solid section background
→ smokey gradient transition
→ background image

Example concept:

linear-gradient(
rgba(255,255,255,0.95) 0%,
rgba(255,255,255,0.85) 25%,
rgba(255,255,255,0.45) 50%,
rgba(255,255,255,0.1) 70%,
rgba(255,255,255,0.0) 100%
),
url(sectionImage)

This creates the cinematic “smoke into image” effect.

3. Placeholder Images

Use placeholder image variables for now. I will replace them later.

Example:

const sectionBackgrounds = {
projects: "PROJECT_IMAGE_URL",
certificates: "CERTIFICATE_IMAGE_URL",
education: "EDUCATION_IMAGE_URL",
competitiveProgramming: "CP_IMAGE_URL"
}

4. Image Positioning

Use consistent styling for all section backgrounds:

background-size: cover
background-position: right center
background-repeat: no-repeat

Images should appear mostly on the **right side**, while content remains readable on the left.

5. Maintain Readability

Ensure the section text remains readable:

• keep a solid background behind content
• use gradient blending to reveal the image
• avoid strong contrast behind text

6. Reusable Styling

Create a reusable style or wrapper component (for example: `CinematicSectionBackground`) so the same background blend pattern can be applied across multiple sections.

7. Maintain Current UI

Do NOT modify:

• hero section
• typography
• spacing
• layout
• animations

Only extend the visual style of sections below the hero.

Goal:
Create a consistent cinematic portfolio where each section softly blends into a background image using the same smokey gradient style as the hero section.
